#include<stdio.h>
int main(){
  int two=2,three=3,six=6;
  int result;
  result = (two + three) * six / three;
  printf("I konw how to write expression : (%d + %d) * %d / %d",two,three,six,three);
  printf("and the result is %d",result);
  return 0;  
}
